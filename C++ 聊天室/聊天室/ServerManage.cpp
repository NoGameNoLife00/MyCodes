#include "Config.h"
#include "stdio.h"

int SendGroupMassage(char *buf, int bufLen)
{
	//int nRet = SUCCESS;
	//DataHead dataHead;
	//GroupChat *gChat = (GroupChat*)buf;
	//char *pFromUser = buf + sizeof(GroupChat);
	//dataHead.sign = DEFAULT_SIGN;
	//dataHead.cmd = CMD_GROUPCHAT;
	//dataHead.dataLen = bufLen;
	//printf("%s 发送了群消息\n", pFromUser);
	//for (;; )
	//{
	//	int iResult = SendHead(userSocket, &dataHead);
	//	if (iResult == SUCCESS)
	//	{
	//		if (send(g_User[i].userSocket, buf, bufLen, 0) < 0)
	//		{
	//			printf("send to [%s] failed!\n", g_User[i].userIP);
	//			nRet = ERROR_SEND;
	//		}
	//	}
	//}
	//return nRet;

	int nRet = SUCCESS;
	return nRet;
}